# frontend

## 环境配置

1. 安装`Node.js`, 版本为`v22.21.1`, 默认附带`npm`;
2. 进入`frontend/`目录, 执行`npm ci`;
3. 执行`npm run dev`, 即可在 **5173** 端口上访问前端页面.

**在`frontend/`目录下创建`.env`并配置以下字段：**

```
# 开发环境无需改动该值
VITE_API_BASE_URL=http://localhost:8000/api/

```
